export { MergeConflictResolver } from "./MergeConflictResolver/MergeConflictResolver";
export type {
    ConflictSelection,
    MergeResolution,
    ResolutionOption,
} from "./MergeConflictResolver/types";
export type { MergeConflict, MergeConflicts } from "../../../api/types/merge.types";
